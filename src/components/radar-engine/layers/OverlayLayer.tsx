import type { RadarLayerProps } from './types';

/**
 * Placeholder for future overlay plugins (weather, airports, heat map,
 * receiver coverage, etc. — see Checkpoint 5's future-overlay
 * interfaces). Renders nothing until plugins are registered.
 */
export function OverlayLayer(_props: RadarLayerProps) {
  return null;
}
